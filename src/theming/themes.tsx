import defaultTheme from './themes/default';
import themeNames from './themeNames';
import blueTheme from './themes/blue';

const themes = {
  [themeNames.DEFAULT]: defaultTheme,
  [themeNames.BLUE]: blueTheme,
};

export default themes;
